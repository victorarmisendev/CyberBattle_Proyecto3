namespace Menuu {
    public class VolumeSlider : SliderBase {

        // Empty for the moment. We need everything in SliderBase, but not sure
        // what else we need for volume sliders. Ideally this should set the
        // slider value to the actual value set in the mixer, but that would
        // require a separate class per audio slider?

    }
}
